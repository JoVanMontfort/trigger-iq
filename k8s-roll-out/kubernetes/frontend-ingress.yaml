apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: triggeriq-frontend-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: frontend.triggeriq.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: triggeriq-frontend-service
                port:
                  number: 80
